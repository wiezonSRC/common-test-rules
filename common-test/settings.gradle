pluginManagement {
    repositories {
        mavenLocal()
        gradlePluginPortal()
        mavenCentral()
    }
}

rootProject.name = 'common-test'

include 'rule-core'
include 'rule-gradle-plugin'
include 'sample-target'