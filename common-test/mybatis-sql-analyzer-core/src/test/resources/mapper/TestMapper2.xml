<?xml version="1.0" encoding="UTF-8"?>
<mapper namespace="com.example.mapper.TestMapper2">
    <select id="findBadPerformancePayments" resultType="map">
        SELECT * FROM payments WHERE payment_id = #{id}
    </select>
    <select i="findTest" resultType="map">
        SELECT *
        FROM payments
        <include refid="com.example.mapper.TestMapper.whereCondition">
        </include>
    </select>
</mapper>
